import React from 'react';
import Space from './Space';

function Equals(props: {}) {
  const { } = props;

  // ---------------------------------------------------------------------------

  return (
    <span className='equals'><Space/>=<Space/></span>
  );

}

export default Equals;