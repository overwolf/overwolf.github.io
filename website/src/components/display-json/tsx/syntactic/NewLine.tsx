import React from 'react';

function NewLine(props: {}) {
  const { } = props;

  // ---------------------------------------------------------------------------

  return (
    <>{'\n'}</>
  );

}

export default NewLine;