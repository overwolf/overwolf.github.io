import React from 'react';
import Space from './Space';

function Arrow(props: {}) {
  const { } = props;

  // ---------------------------------------------------------------------------

  return (
    <span className='arrow'><Space/>{"=>"}<Space/></span>
  );

}

export default Arrow;