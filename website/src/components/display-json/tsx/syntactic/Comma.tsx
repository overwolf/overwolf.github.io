import React from 'react';

function Comma(props: {}) {
  const { } = props;

  // ---------------------------------------------------------------------------

  return (
    <span className='comma'>,</span>
  );

}

export default Comma;