import React from 'react';

function Template(props: { }) {
 const { } = props;

// ---------------------------------------------------------------------------

  return (
    <>
    </>
  );

}

export default Template;