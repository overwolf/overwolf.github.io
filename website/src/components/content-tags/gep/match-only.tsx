import React from 'react';

function MatchOnly(props: {}) {
    const { } = props;

// ---------------------------------------------------------------------------
    // TODO - Make this dynamically fetched from tags json
    return (
        <div>
            <b>This event is intended for real-time usage only, and its content should not be logged.</b>
        </div>
    );

}

export default MatchOnly;