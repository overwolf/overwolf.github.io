import './preloader.scss';
import React from 'react';

function Preloader(props) {
 const { } = props;

// ---------------------------------------------------------------------------

  return (
    <p>loading...</p>
  );

}

export default Preloader;