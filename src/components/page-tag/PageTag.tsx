import { CustomPropsPageType } from '@site/../src/navigation/sidebar/types/customPropsPage';
import React from 'react';

export default function PageTag(props: CustomPropsPageType['tags']) {
  return (
    <div>
        
    </div>
  );
}