// const fs = require('fs') // filesystem
// const path = "cli/helpers/temp-redirects.json"

// if (Object.keys(JSON.parse(fs.readFileSync(path, 'utf8'))).length > 0) {
//     throw new Error("Redirects were not properly committed!")
// }
// else console.log("Redirects are properly committed");